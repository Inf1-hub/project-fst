# 《逆光》地图生成设计

> 文档版本：v1.6  
> 隶属：《逆光设计文档》v3.2 的关卡空间补充，不改动 52 场景名额、零件库合同与 Ability–Effect 铁则  
> 参考生成逻辑：**流放之路 / 流放之路 2**（只抽「约束下的随机」质量思想，不借暗金词缀、地图装置与版权专名）  
> 兼容对象：既有**车道式单向走廊**关卡原型（主通道保底通关）  
> 引擎落地：Godot 4.7.2，逻辑层用网格；视觉层仍套遗迹手绘零件，禁止用无限平铺小格子冒充场景

---

## 0. 文档定位

主设计文档 §3 规定：第 1–9 层是「若干战斗簇 + 连接路径 + 1 个 Boss 庭院」，场景走哈迪斯方案（10 套零件库 × 5 种房间拓扑）。本文回答的是下一层问题：**在一块有限的可生成范围内，如何先长出一块够大的不规则地图内容，再在这块内容里扣出可走地，且每次随机都不会崩坏。**

核心一句：

> 质量来自约束下的随机：先规定「什么必须为真」，再只在合法空位里掷骰。拓扑先于地形，零件必须对上接口，结构节点不能丢，失败就整图重来。随机只发生在已知节点之间怎么走，不发生在随便挖。

本文不管层数曲线、余晖掉落上限、组织编制——那些仍读主文档与 `data/`。本文只管**生成范围、拓扑图、零件接口、地图内容、可走地、骨架、连通、禁刷、迷雾、高低台、生成质量**。

质量铁则见 §4.3。后面所有步骤都服从那张表，表和细则冲突时以铁则为准。

| 本文管 | 本文不管 |
| --- | --- |
| 可生成范围、拓扑图、零件接口、地图内容、不规则可走地、起点终点、主路径、侧室、高程、迷雾、刷点距离、质量铁则 | 追光者数值、余晖三选一、词缀、光阶开门 |
| 生成失败重试 | 营地 `CAMP`、日冕 `CORONA`（不走本生成器） |
| 与 L1–L5 拓扑的权重对接、地形零件美术合同 | 为 50 张关卡各画一张整图 |

---

## 1. 必须先分清的四层空间

这一条写错，后面所有规则都会歪。生成器里有四层完全不同的东西，**禁止把「地图内容」和「可走地」算成同一个数**。

| 层 | 正式名 | 是什么 | 玩家看不看得到 |
| --- | --- | --- | --- |
| 算法层 | **可生成范围** | 生成器允许落点的逻辑工作区。默认正方形网格，只用来限范围、算象限、防溢出 | **看不到**。不是墙，不是边框，不是场地轮廓，场景里禁止画成一圈规则矩形 |
| 内容层 | **地图内容** | 范围内真正长出来的地：地面、墙、山体、悬崖收边、障碍、高台，全部算 | 看得到。这才是小地图剪影，对齐流放之路 2「这张图有多大」 |
| 行动层 | **可走地** | 地图内容里玩家能站、能打的格子。墙、山体、障碍从内容里扣掉之后剩下的 | 看得到。比地图内容小，但不能小成一条细走廊 |
| 空白层 | **虚空** | 范围内没长出任何内容的格子，取不到、也没有山墙 | 基本看不到（雾或空）。只用来咬出不规则外缘，禁止占大半张图 |

对齐流放之路 2 必须写死的一点：

> 可生成范围只是「这一次最多能在哪一块逻辑区里长地图」。玩家看到的是一块够大的不规则**地图内容**（地面 + 墙 + 山体 + 障碍），不是细走廊漂在空白里。墙和山体会降低可走空间，所以可走地 < 地图内容；但地图内容本身必须明显超过范围的一半，可走地也必须仍是一块能打仗的地，不能只剩「这么一点」。

因此：

- **地图内容**必须占可生成范围的大部分，且外轮廓不规则。
- **可走地**是内容的子集，被墙 / 山体 / 障碍削减，但削减后仍要够用。
- 虚空只出现在内容外侧的缺口和角落，用来防止方场；禁止把半张图留成虚空再叫「不规则」。
- 禁止在场景或小地图上画出可生成范围的边线（调试叠加除外）。
- 「孤岛」只禁止**可走却到不了**的房间；虚空不是孤岛。墙和山体是地图内容，不是虚空。

示意（`·` = 虚空，`#` = 可走地，`X` = 墙 / 山体 / 障碍，同属地图内容）：

```
可生成范围（仅算法，玩家不可见）
································
····XXXXXXXXXXXXXX··············
···XX######XXXXXXX······XXXX····
····XX##··######XXX···XXX##XX···
······XXXX···######XXXX####XX···
············XXX##XX######XXX····
··········XXXX######XXXXXX······
··········XXXXXXXXXXXX··········
································
```

小地图剪影看的是 `#` + `X` 连成的整块地，不是只看 `#`。

---

## 2. 核心思路

对齐流放之路 2 的可玩生成，而不是开箱随机噪声，也不是铺一块方场地：

1. **划定可生成范围**：给算法一块有限工作区，内容不得溢出；范围本身不是地图。
2. **先画拓扑图**：必经节点（起点、终点、主将庭院）先钉死；可选节点（侧室、回路）可生成也可丢，但不能把主图扯变形。
3. **主骨架按边去铺**：只在图的边上挖车道，复用单向走廊；随机是「边怎么走」，不是满图噪声。
4. **长出地图内容**：沿骨架和房间向外长墙、山体、收边、障碍，形成一块够大的不规则地；不要停在「只挖了路」。
5. **再削减可走**：墙、山体、障碍留在内容里，把可走地从整块地里扣出来。
6. **硬约束与质量铁则优先**：§4 与 §4.3 任一失败，丢弃本次结果，整图重生成。禁止打补丁。
7. **小图、单连通**：整张图的体量约为流放之路 2 常规关卡的 **1/3**（比的是地图内容，不是虚空）；可走格必须彼此连通，禁止可走孤岛。
8. **迷雾 + 三层高程**：探索感来自揭雾；高低台只做 3 层，且不能切断主路。高程只许用带台阶的零件，禁止算出来的非法落差。

```
划定可生成范围（算法工作区，不渲染边线）
    │
 画拓扑图：必经节点钉死，可选回路 / 侧室挂边
    │
 范围内选起点 / 终点（距离 + 象限硬约束）
    │
 只沿图的边挖车道式主路径（S 形 / 缓折）
    │
 可选边 → 0~3 个侧室或回路（回路必须回到主路）
    │
 用带接口的零件拼几何；非法接口直接失败
    │
 沿骨架生长地图内容（地面 + 墙 + 山体 + 障碍）
    │
 用墙 / 山体 / 障碍削减可走，虚空只留在内容外侧
    │
 三层高程（只用台阶零件，不切主路，起点强制平地）
    │
 安全区锁定 → 奖励 → 怪物簇
    │
 迷雾初始化（按地图内容揭剪影，按可走地加载实体）
    │
 全量硬约束校验 → 失败则整图重生成
```

---

## 3. 地图规格

### 3.1 可生成范围（算法工作区）

| 项 | 规格 | 说明 |
| --- | --- | --- |
| 性质 | 生成器的落点上限，不是地图外形 | 只回答「这一次最多能在哪生成」。不是外框、不是墙、不是玩家场地 |
| 默认形状 | 正方形逻辑网格 | 选正方形只为算象限和距离方便；可配成长方形。形状不得出现在玩家视野里 |
| 建议边长 | **48 格**（可配 40 / 48 / 56） | 一格 = 碰撞寻路单元，不是美术小瓦片 |
| 溢出规则 | 房间、通道、高程、刷点不得落在范围外 | 超出即非法；范围内留白、挖空、挡死都合法 |
| 可见性 | 运行时不绘制范围边界 | 调试叠加可画虚线，正式场景 / 小地图禁止出现规则矩形边 |

### 3.2 地图内容（玩家看到的那块地）

地图内容 = 地面 + 墙 + 山体 + 悬崖收边 + 障碍 + 高台。小地图剪影看的是这一层，不是只看能走的地板。

| 项 | 规格 | 说明 |
| --- | --- | --- |
| 来源 | 沿主路径和房间**生长出来** | 先有骨架，再把两侧和房间外壁长成一整块地 |
| 轮廓 | 不规则，对齐流放之路 2 | 禁止规则矩形、禁止四边贴齐、禁止铺满整个可生成范围 |
| 面积锚点 | 约等于流放之路 2 常规关卡**地图内容**的 **1/3** | 比的是「这张图有多大」，含墙和山，不是只比能站的地 |
| 内容填充率 | 内容格 / 范围格 **62%–80%**，目标约 **70%** | 流放 2 一张图的内容明显超过一半。低于 62% 是细地漂在虚空里 |
| 连通性 | 内容块自身连成一片 | 允许内容里有墙洞，但不允许两块互不挨着的「飞地内容」 |

默认 48×48 范围、70% 内容 ≈ **1610 格**有地。墙和山都算在这 1610 里。

### 3.3 可走地（从地图内容里扣出来）

| 项 | 规格 | 说明 |
| --- | --- | --- |
| 来源 | 地图内容减去墙 / 山体 / 障碍 | 可走地 ⊂ 地图内容。障碍是在削减可走，不是把整张图删掉 |
| 可走 / 内容 | **68%–85%**，目标约 **78%** | 大约两成内容是墙山障碍，八成还能走。再低就只剩细路 |
| 可走 / 范围 | 由此约 **48%–64%**，目标约 **55%** | 这是扣完障碍后的站立空间，不是「地图只有一半」 |
| 连通性 | 可走格单连通 | 所有可走房间必须能走到；墙和虚空不必连通 |
| 主通道宽度 | **4 格**（最小通行 3 格） | 兼容既有车道式走廊；中间可插 1 格隔离墙，两侧仍可走 |

同样 48×48、内容 70%、可走占内容 78% 时：可走约 **1250 格**，墙山障碍约 **360 格**，虚空约 **690 格**（只咬外缘）。这才是「有一块大地，里面有挡路的东西」，不是「只有一千格路」。

视觉层仍用该遗迹的手绘零件盖在**地图内容**上（地面大块、墙、门、山、障碍）。虚空和范围外侧用雾 / 远景收，收边必须跟着**内容轮廓**走，禁止收成一条直线围墙。`TileMapLayer` 只许铺碰撞或廉价重复边，英雄地面必须是手绘大块。镜头锁死、只平移，与主文档 §3.0.1 一致。

### 3.4 与既有关卡原型的关系

| 既有概念 | 在本生成器中变成 |
| --- | --- |
| 车道式单向走廊 | **主路径骨架**（必生成，保底通关） |
| L1 前哨 / L2 据点 / L3 裂道 / L4 深庭 / L5 祭庭 | **生成画像**：改蜿蜒程度、侧室数、高程占比、簇密度，不另写五套生成器 |
| 战斗簇 | 主路径弯道 / 高低变化处 / 侧室内的刷点 |
| 精英门 | 侧室类型之一（精英房），微弱红光，不挡主路 |
| 安全屋 | 侧室类型之一（事件房的子集），仍不占 52 场景名额 |
| 本层主将庭院 | 主路径终点房间，击杀前光阶不升起 |
| 出生锚点 | 起点 + 半径 A / B 安全区（只覆盖可走地） |

同一张生成图在第 2 层与第 8 层只换编制与数值，不换生成器。营地与日冕不走本流程。

### 3.5 坐标系与象限

象限只相对于**可生成范围的中心**，用来约束起点 / 终点，不表示玩家场地被切成四块。

| 象限 | 范围（相对可生成范围中心） |
| --- | --- |
| I | x ≥ 0, y ≥ 0 |
| II | x < 0, y ≥ 0 |
| III | x < 0, y < 0 |
| IV | x ≥ 0, y < 0 |

落在轴上的格，按「先 x 再 y」归入相邻象限。实现时必须写死同一套归属规则。起点终点可以都靠近范围边缘，但可走地本身不必贴边。

---

## 4. 全局硬性约束（优先级最高）

生成中途与生成结束都必须校验。**任意一条不满足 → 丢弃本次结果，重新生成整张地图。** 禁止局部打补丁硬塞房间。

| 类别 | 规则 | 目的 |
| --- | --- | --- |
| 地图内容体量 | 内容格 / 范围格必须落在 62%–80%；内容外轮廓不得接近规则矩形（见 §4.1） | 先保证「有一张够大的地」，对齐流放 2 |
| 可走削减 | 可走格 / 内容格必须落在 68%–85%；墙、山体、障碍只能从内容里扣，不能把内容删成虚空 | 障碍降低行动空间，但不许把地图削没 |
| 起点 / 终点距离 | 1. 欧氏距离 ≥ 可生成范围边长的 **70%**（默认 48 格则 ≥ 33.6 格）；2. **禁止同一象限**；3. 禁止直接相邻；4. 从起点沿主通道直线视野**不能一眼看见终点**（中间必须有弯折或遮挡） | 防止地图太短、一眼通关 |
| 起点安全区 | 1. 半径 **A 格**：禁止刷任何怪物；2. 半径 **B 格**（B > A）：禁止刷奖励、宝箱、精英、事件；3. 安全区只作用于**可走地**；4. 区内只允许基础地面、装饰；禁止交互资源；5. 区内强制全部平地（高程 = 0） | 开局缓冲，落地不被围，出门不直接捡完奖励 |
| 可走连通性 | 所有可走房间 / 区块必须连通；**不允许可走孤岛**。虚空只留在内容外侧。每个可选侧室有且仅有 **1** 条通道连回主路径 | 奖励可达；墙和山不是孤岛 |
| 主路径下限 | 必须挖出一条**连续主通路**（车道式单向骨架），保证玩家一定能从起点走到终点；侧室只能挂靠，不能切断主线 | 保底通关路线 |
| 生成范围 | 所有房间、通道、高程、刷点不能落到可生成范围之外 | 防溢出；不是给玩家砌围墙 |
| 迷雾规则 | 1. 初始：除起点安全区外，地图内容全部黑色迷雾；2. 视野：玩家周围圆形视野，进入才揭开；3. 已揭开的内容（含墙和山）永久保留；4. 未揭开不预加载怪物 / 奖励数据；5. 迷雾跟着内容轮廓走，不按可生成范围切一块大方黑 | 保留探索未知感 |
| 高程通行 | 只允许相邻层互通（0↔1、1↔2）；落差 > 1 视为墙；主路径全程可通行 | 小地图不做成多层迷宫 |
| 自交与穿墙 | 主路径不得自相交、不得穿已标死的不可走实心 | 防止车道打结 |
| 质量铁则 | 必须同时满足 §4.3 全部原则。非法接口、结构节点丢失、空走过久、误导性死胡同、意外细缝、一张图多种宏观结构，均整图失败 | 保下限，不靠碰运气 |

### 4.1 不规则轮廓的验收（硬规则）

生成结束后必须同时满足，否则整图重生成：

1. **内容填充率**：地图内容格 / 可生成范围格 ∈ [0.62, 0.80]，默认朝 **0.70** 靠。低于 0.62 是细地漂在虚空里，不像流放 2。  
2. **可走占内容**：可走格 / 内容格 ∈ [0.68, 0.85]，默认朝 **0.78** 靠。低于 0.68 说明墙山把路堵死了；高于 0.85 说明内容里几乎没有障碍，太平。  
3. **贴边率**：内容格落在范围最外一圈的比例 < **8%**。禁止整块地顺着四条边长出来。  
4. **轴对齐矩形度**：地图内容的轴对齐包围盒面积 / 内容格数 > **1.35**。过接近 1.0 就是一块实心方场。防方场靠内容和外缘虚空，不靠把整张图留白。  
5. **四角空洞**：可生成范围的四个角落各取 8×8，其中至少 **3** 个角落**内容格**占比 < 20%。角落应是虚空或稀薄收边，不是整角铺满。  
6. **视觉收边**：内容外侧用山体 / 废墟 / 雾收边，收边折线不得是四条长直线。墙和山算内容，必须画在剪影里。

调试叠加可以画可生成范围；正式验收看的是**地图内容剪影**（含墙和山），不是范围方块，也不是只剩地板的细线。

### 4.2 安全区默认半径

| 参数 | 默认 | 约束 |
| --- | --- | --- |
| A（禁怪） | **6 格** | 必须盖住出生点与第一段直道入口 |
| B（禁奖励 / 精英 / 事件） | **12 格** | 必须 B > A；B 不得大到把主路径中段吃掉 |

A / B 进配置表。校验用欧氏距离，但只锁定**可走格**；圆和墙 / 山 / 虚空相交的部分不算「安全区里还能走」。

---

## 4.3 地图质量铁则

从流放之路的生成里抽出来的不是某几条技法，而是下面这套思想：**随机只填合法空位；结构先于装饰；一张图一种语言；非法结果不许出场。** 公开来源包括 ExileCon 的世界生成讲座、墓地地图「由内向外翻」的修复、以及流放 2 地图主策对布局可读性的说明。下面写成**本生成器的宪法**，不绑定他们的工具名。后文任何细则与此表冲突，以此表为准。

### 4.3.1 原则总表

| # | 原则 | 一句话 | 本生成器怎么守 |
| --- | --- | --- | --- |
| Q1 | **约束先于随机** | 先写死「什么必须为真」，再只在合法空位掷骰 | 没有拓扑图、没有零件键、没有宽度合同，禁止开始挖地 |
| Q2 | **拓扑先于几何** | 先决定点与点怎么连，再铺看起来像地的东西 | 第 0 步出节点图；禁止直接在 48×48 上噪声挖 |
| Q3 | **结构不可丢，装饰可丢** | 撑起形状的节点必须在；可选物不刷时主图不能瘪、不能翻 | 起点 / 终点 / 主将庭院必在。丢掉可选边后主路长度下降 ≤ 15%，外轮廓不得翻面 |
| Q4 | **一张图一种语言** | 单张图内部结构要统一；口味差放在图池里换 | 宏观只抽一种：蜿蜒脊柱 / 缓折脊柱 / 脊柱+短回路。禁止一张图混迷宫、大广场、细缝 |
| Q5 | **推进可读，选择局部** | 玩家始终知道往哪走；丰富来自主线旁的小决定 | 只允许绕障、短回路、侧室三类。禁止二选一长支路 |
| Q6 | **只拼合法零件** | 邻接对不上的几何，根本不该成为候选 | 四边接口合同；无匹配键则整图失败，禁止硬塞 |
| Q7 | **先键后皮** | 逻辑键决定能不能接；皮只负责不像复读 | 同键多变体抽外观。一块皮不能独特到无法复用，也不能张张同一张 |
| Q8 | **接缝合并，禁止补缝** | 房间与路重叠后按表合成，不在空隙里后补门 | 重叠 1 格。可走+虚空 = 非法 |
| Q9 | **可玩优先于像真** | 卡住、过窄、看不懂，比「不够写实」更不可接受 | 主路净宽 ≥ 3；无冲刺也应能过。卡口必须打标，禁止意外细缝 |
| Q10 | **卡口必须是故意的** | 窄是设计，不是生成器挤出来的 | 未打标处截面不得 < 3 格。打标卡口两侧必须能看见汇合 |
| Q11 | **死胡同不得像主路** | 走错的路要一眼可辨，或很快回到主线 | 死胡同深度 ≤ 6 格，或入口有光/缺口。看起来像主路的岔必须是回路 |
| Q12 | **沿主线有节奏** | 空走太久和兴趣点扎堆一样糟 | 主路上每 10–16 格至少 1 个兴趣拍（弯道、绕障、簇、高低、口子）。连续空走 > 18 格失败 |
| Q13 | **兴趣点钉在节点上** | 奖励、庭院、侧室跟拓扑走，不往空白里撒 | 刷点必须落在已有节点 / 边的可走格，禁止脱离骨架的散点 |
| Q14 | **功能有预算** | 一张图不把所有花样堆满 | 回路与侧室互斥优先；高程高档与 3 侧室不同时抽满 |
| Q15 | **宽度连续** | 路可以弯，不能突然瘦成缝再突然胖 | 相邻截面宽度差 ≤ 2 格，除非经过打标卡口 |
| Q16 | **路与画分离** | 碰撞和寻路是逻辑层，美术不得改可走性 | 手绘块不许溢出自己的键。改手感改键，不改像素墙 |
| Q17 | **出界即不可走** | 碰到可生成范围外，自动收成山/雾 | 不得留一条能走出去的缝，也不画范围围墙 |
| Q18 | **高差只走零件** | 高度不是算出来硬接的 | 只许 0↔1、1↔2，且必须经台阶键。平地邻 +2 = 失败 |
| Q19 | **失败封闭** | 不合法就不给玩家，禁止打补丁凑活 | 任一条失败整图重来，最多 24 次。禁止删一堵墙、挪一个房来「救」 |
| Q20 | **可复现、可看见、可度量** | 同种子同图；bug 先看键和图；成功率要记账 | 调试必须能画拓扑 / 键 / 掩码。记录生成成功率，低于 60% 先改约束或零件库 |
| Q21 | **战斗空间够用** | 能连通不等于能打仗 | 除纯走廊段外，每 1 个战斗簇附近须有 ≥ 6×6 的可走口袋（可含绕障，但人能散开） |
| Q22 | **边要加权** | 节点之间怎么走也有成本，不是最短折线 | 贴范围边、贴已有房间、急转 90° 连续两次，成本升高。弯道用预做弯道零件收 |
| Q23 | **灰盒能玩再套皮** | 逻辑可走、可打、可读，才允许换手绘 | 无皮验收必须能走完主线。皮只许换外观，不许改边界 |
| Q24 | **路标可辨** | 可选内容的入口要能从主路认出来 | 回路口、侧室门须有缺口或微光。禁止「墙缝里藏了一整间房」 |

### 4.3.2 结构与可选：怎么丢而不塌

| 图上的东西 | 作用 | 丢掉时 |
| --- | --- | --- |
| 必经节点 | 起点、终点、主将庭院 | 不许丢。缺一即失败 |
| 结构锚点 | 撑外轮廓、防翻面的 2–4 个空节点（不放玩法） | 不许丢。没有它们，可选物一少，地会往里抽 |
| 可选节点 | 侧室、短回路、额外奖励 | 可丢。只删该点及其边，主边几何不动 |
| 边 | 两点必须走通的约束 | 随机只发生在「沿边怎么走」 |

禁止用可选节点把图撑大。可选物的职责是挂在已经成立的主边上，不是当梁。

### 4.3.3 路线选择（Q5 的唯一合法实现）

| 类型 | 怎么生成 | 上限 | 失败时 |
| --- | --- | --- | --- |
| A. 绕障 | 主通道内放隔离墙 / 山石，左右都能汇合 | 每 12 格最多 1 处；全图至少 2 处 | 会堵死主路则不放 |
| B. 短回路 | 从主路出去，必须在前方回到主路 | 0–2 条；长度 ≤ 已走主路的 25% | 接不回则整条丢，主路不动 |
| C. 侧室 | 单通道挂回主路 | 0–3，权重偏 1–2 | 接不上则弃接口 |

已有 2 条回路时，侧室期望降为 0–1（Q14）。  
错边代价：绕障 < 3 秒，回路 < 8 秒回到主路，否则失败（Q9、Q11）。

### 4.3.4 零件合同（Q6–Q8、Q16–Q18）

遗迹手绘零件库就是图块。逻辑层只摆键，视觉抽同键变体。

| 规则 | 说明 |
| --- | --- |
| 四边接口 | 可走 / 墙 / 山体 / 虚空 / 台阶(0↔1) / 台阶(1↔2)。对不上禁止拼接 |
| 角状态 | 四角也要声明，防止斜向穿缝 |
| 重叠合并 | 可走+可走=可走；墙+墙=开门或薄墙（查表）；可走+虚空=非法 |
| 弯道 | 折角盖预做弯道零件，禁止 1 格锯齿墙 |
| 出界 | 内容触到范围外，该边收成山体 / 雾 |
| 无匹配键 | 不准硬塞，整图重来 |

### 4.3.5 校验闭环（Q19–Q20）

1. 拓扑自检：必经 + 结构锚点都在；去可选边后主边连通且长度不暴跌。  
2. 几何自检：接口、重叠、宽度连续、卡口打标、高程零件、出界。  
3. 体验自检：一种宏观语言、节奏拍、战斗口袋、路标、死胡同、§4.1 轮廓、安全区、密度。  
4. 任一步失败整图重来，最多 24 次。  
5. 同 `run_seed + floor_index + layout_id` 可复现；重试用子种子。  
6. 日志记：成功率、失败条款、平均重试。成功率 < 60% 先改零件库或约束，不准靠加长重试次数瞒过去。

出地形 bug 的顺序：看拓扑是否翻面 → 看哪条边键对不上 → 再看美术。不要先改图。

---


## 5. 路径与房间（先画图，再挖骨架，再填内容）

流放之路的可玩部分：先有节点图，再沿边铺房间 / 车道，再把零件键换成手绘块，最后才填怪物与奖励。虚空只留在内容外侧。本生成器同一顺序。

### 5.1 主路径生成

1. 可生成范围初始为**全虚空**。用带偏移的 S 形 / 蜿蜒车道从起点挖到终点，复用既有车道式走廊：固定宽度、单向前进、中间可生成隔离墙。
2. 主路径形态每次从权重池抽取（见 §8）：蜿蜒车道 或 缓折走廊，都保持单向前进，禁止环形主线。
3. 沿主路径每隔固定距离预留 **分支接口点**（默认每隔 8–10 格评估一次），用来挂可选侧室；接口可生成也可空置。
4. 主路径不得自相交、不得挖出范围、不得为了「填满」去贴边。
5. 挖完立刻校验：主通道任意截面可通行宽度 ≥ 3 格。随后必须进入「生长地图内容」步，禁止停在只挖了路。

车道中间的隔离墙是装饰与拆组用的，不能把主通道截成断头路。隔离墙两侧必须仍能绕行汇合。

### 5.2 分支侧室（随机挂载）

| 规则 | 说明 |
| --- | --- |
| 非必刷 | 接口点按概率生成，不是每个接口都出房 |
| 数量 | 0–3 个；权重偏向 1–2（见 §8） |
| 出口 | **强制回到主路径**；不做「二选一长支路」，避免结构失控 |
| 通道数 | 每个侧室有且仅有 1 条通道连回主路径 |
| 尺寸 | 随机，但边长上限默认 **10 格**，面积上限默认 **80 格**；禁止过小碎房间（短边 < 4 格或面积 < 16 格） |
| 类型 | 精英房 / 商店 / 献祭事件（含安全屋子集） |
| 外形 | 允许矩形房间作为内部碰撞，但房间外墙必须带凹凸 / 错位，不得在可走剪影上露出一整块方印 |

侧室挂在主路径侧面，不得穿越主通道，也不得为了对称把范围两侧都铺满成方块。侧室自身必须单连通。侧室外壁和主路径两侧都要长出地图内容（墙、山、收边），不要把侧室孤零零丢在虚空里。

### 5.3 房间形态

房间只分两类：

| 类型 | 用途 | 形态 |
| --- | --- | --- |
| 走廊通道 | 主路径、侧室连接道 | 固定宽度车道；允许缓折与隔离墙 |
| 独立房间 | 起点缓冲、侧室、终点庭院 | 内部可矩形或带凹凸；外轮廓禁止一眼看穿的大方块；禁止碎房间 |

连接通道最小宽度固定（默认 3 格），防止细缝。房间之间禁止只靠对角 1 格连通。  
房间是地图内容里的局部，不是把可生成范围切成九宫格。房间与走廊必须重叠 1 格并走 §4.3.4 的合并表，禁止中间空一缝再「补门」。

### 5.4 地图内容生长与障碍削减

骨架和侧室就位后，必须再跑这一步，否则图会瘦成走廊：

1. 沿主路径和房间外缘向外生长 2–4 格，标记为地图内容（地面或收边山体）。  
2. 在内容内部铺墙、隔离石、山体、高台底座。这些格仍是内容，但标成不可走。  
3. 隔离墙不能截断主通道；山体优先贴内容外缘和房间侧边，不挡在起点安全区。  
4. 生长结束后校验：内容填充率、可走占内容、主路仍可通行、所有邻接零件键合法。失败则整图重试，禁止只删障碍凑数，禁止换一块接口不对的零件硬塞。

### 5.5 路线选择怎么放才丰富、不乱

落实 Q5、Q11、Q12、Q14、Q24。丰富指走路时有决定，不是多几个互不相干的房间。细则以 §4.3.3 为准。

1. 主边可读：感到「终点在前方」，靠弯折和遮挡藏终点，不靠分叉迷惑。  
2. 至少 2 处绕障；已有 2 条回路则侧室降为 0–1。  
3. 回路口、侧室门必须从主路能认出来。  
4. 错边 3 秒 / 8 秒内能回；死胡同不像主路。  
5. 绕障两侧不要对称刷满；高价值放回路或侧室，主路保底推进。

---

## 6. 内容刷新（怪物、奖励、事件）

刷点必须落在已连通、非墙、非悬崖、且通过安全区校验的**可走格**。虚空和墙山格不刷任何交互物。未揭开迷雾的可走地**不实例化**怪物与奖励实体，只保留生成期写入的刷点数据；玩家揭雾后再加载。

### 6.1 奖励

| 规则 | 说明 |
| --- | --- |
| 禁区 | 不得落在起点半径 B 内的可走格 |
| 互斥距离 | 两个奖励点欧氏距离 ≥ **8 格**，禁止扎堆 |
| 优先位置 | 主路径中段、侧室内部 |
| 终点保底 | 终点庭院附近固定 **1** 个保底奖励（可被本层掉落上限折算，但点位必须在） |
| 与怪物 | 奖励格及其 2 格邻域禁止刷怪物簇中心 |

奖励包括：宝箱、神佑（余晖掉落锚点）、事件祭坛。具体掉落次数与品质仍读主文档层数曲线。

### 6.2 怪物

| 规则 | 说明 |
| --- | --- |
| 禁区 | 不得落在起点半径 A 内的可走格 |
| 离奖励 | 簇中心与奖励点距离 ≥ **4 格**，不能刷在宝箱上 |
| 走廊优先 | 弯道、高低地形变化处；尽量避免长直车道堵门 |
| 精英 | 只允许：侧室（精英房），或主路径**远离起点的后半段**（路径进度 ≥ 50%） |
| 密度上限 | 规划的怪物格占比 ≤ **12% 可走格**（分母是可走地，不是地图内容，也不是可生成范围） |

主路径前半段只允许普通簇。号手 / 纠察走巡逻线，沿线不得穿过 A 区，也不得走进墙、山或虚空。

### 6.3 密度与编制

生成器只决定**点位与簇槽**。槽里填哪套职业对齐模板、多少只，读层数、遗迹、难度，不在本文重写。  
本生成器要守住的是：点位合法、不堵门、不超过密度上限、精英不出现在开局。

### 6.4 节奏、路标与战斗口袋（Q12、Q13、Q21、Q24）

几何连通之后还要过体验检，否则会走出「能通但很空、或空地打不开身」的图。

| 规则 | 规格 |
| --- | --- |
| 兴趣拍 | 沿主路每 10–16 格至少 1 个：弯道、绕障、簇、高低变化、回路/侧室口 |
| 空走上限 | 连续无可读变化 > **18 格**，整图失败 |
| 钉在节点 | 奖励、庭院、侧室、保底奖励必须落在拓扑节点或边的可走格上 |
| 战斗口袋 | 每个战斗簇中心 8 格内，须有一块 ≥ **6×6** 的可走区（可含绕障，人能散开） |
| 路标 | 可选入口从主路可见（缺口或微光）；禁止墙缝藏房 |
| 功能预算 | 高程抽「高」档时，侧室上限视为 2；3 个侧室时高程不得抽「高」 |

---

## 7. 高低地形（高程）

不是纯美术。高程附带通行与战斗约束。参考流放之路的高低台与台阶，但小地图只做 3 层。高程只铺在地图内容上；虚空不编造可爬层级。山体和高台算内容，其中不能走的部分计入障碍削减。

### 7.1 分层

| 层 | 值 | 用途 |
| --- | --- | --- |
| 平地 | 0 | 默认地面；起点安全区强制本层 |
| 低台 | +1 | 掩体、凹室、短台阶过渡 |
| 高台 | +2 | 远程怪、宝箱；高地揭雾半径更大 |

禁止 +3 及以上，禁止复杂垂直迷宫。

### 7.2 通行硬规则

- 只允许相邻高度互通：平地 ↔ 低台，低台 ↔ 高台。
- **平地不能直接上高台**，必须经斜坡 / 台阶（占 1–2 格过渡）。
- 相邻格落差 > 1，视为不可通行墙体（悬崖）。
- 高低区块**不能切断主路径**。主线全程可通行；高低台只做房间内与走廊**侧边**的地形变化。
- 起点安全区（半径 B 内的可走格）强制全部平地，禁止低台 / 高台 / 悬崖。

### 7.3 地形生成约束与战斗收益

| 约束 | 说明 |
| --- | --- |
| 占比分档 | 低 10% / 中 25% / 高 40%（相对**可走格**）；高占比权重更低（见 §8） |
| 高台优先 | 远程追光者、宝箱 |
| 低台优先 | 掩体、凹室、近战拆组 |
| 主路保护 | 主通道中线两侧各 1 格优先保持平地或仅 +1，且必须有台阶连通 |
| 视野 | 高地单位揭雾半径系数默认 **1.25**；低地可利用高差躲避平射投射物 |

L3「裂道」画像提高中档高程权重，但仍受 3 层上限与主路不可切断约束。落差用分层绘景 + 分层碰撞表现，不是真 3D 高度场。

---

## 8. 随机权重（有变化，但不崩坏）

每次生成先抽一组**生成画像**，再跑管线。权重保证不会张张一样，也不会侧室和高台同时堆满。

### 8.1 主路径形态

| 形态 | 权重 | 特征 |
| --- | --- | --- |
| 蜿蜒车道 | 55 | 带偏移 S 形，弯道多，适合走廊遭遇 |
| 缓折走廊 | 45 | 折数少、单向更明确，适合前哨 / 据点 |

两者都必须单向前进，禁止环形主线、禁止自交。蜿蜒时用内容外缘的虚空咬出缺口，不要用「把范围填满」或「只留一条路」来制造变化。

### 8.2 侧室数量

| 数量 | 权重 |
| --- | --- |
| 0 | 10 |
| 1 | 40 |
| 2 | 35 |
| 3 | 15 |

类型在已决定数量后独立抽取：精英房 40 / 商店 25 / 献祭事件 35。同一张图内类型尽量不重复；若抽到 3 个侧室，第三个强制与前两个不同。

### 8.3 高低地形占比

| 档 | 高程格 / 可走格 | 权重 |
| --- | --- | --- |
| 低 | 约 10% | 45 |
| 中 | 约 25% | 40 |
| 高 | 约 40% | 15 |

高档权重最低，避免全图高低台乱跑。实际铺设后若切断主路，本档作废，降一档重铺高程；仍失败则整图重生成。

### 8.4 与 L1–L5 画像对接

生成器只有一套，用原型偏置权重，不复制五份代码：

| 原型 | 路径 | 侧室期望 | 高程档 |
| --- | --- | --- | --- |
| L1 前哨 | 偏缓折 | 0–1 | 低 |
| L2 据点 | 缓折或蜿蜒 | 1 | 低 / 中 |
| L3 裂道 | 偏蜿蜒 | 1–2 | 中（可抽到高） |
| L4 深庭 | 蜿蜒 | 2 | 中 |
| L5 祭庭 | 缓折进入大终点房 | 1–2（含终点庭院外的侧厅） | 低 / 中 |

层数对 L1–L5 的抽取仍读主文档 §3.3，本文不改。五种画像只偏置节奏，不另开生成器，也不许一张图内部换多种宏观语言（Q4）。

---

## 9. 生成执行顺序（必须按序，禁止并行乱填）

程序化生成按下列顺序跑。中途失败只允许回退到指定步骤，不允许在已失败的图上继续堆内容。

| 步 | 动作 | 失败回退 |
| --- | --- | --- |
| 0 | 生成拓扑图：钉死起点 / 终点 / 主将庭院 + 2–4 个结构锚点；按画像挂可选边；丢掉可选边后主边连通且长度不暴跌 | 本步重抽；超过 N 次回退换子种子 |
| 1 | 划定可生成范围，范围内先标为虚空；按节点落入范围，不画出范围边 | 节点放不下则回退步 0 |
| 2 | 校验起点终点距离、象限、不相邻 | 本步重选终点；超过 N 次回退步 0 |
| 3 | **只沿主边**按加权成本挖车道；校验宽度连续、不自交、不能一眼看见终点 | 回退步 2 |
| 4 | 沿可选边生成短回路或侧室；接不上则整条丢、主边不动 | 画像下限仍不满足则回退步 0 |
| 5 | 用零件键拼几何并重叠合并；生长内容；削减可走；非法接口不得硬塞 | 接口 / 填充 / 宽度 / 卡口失败则回退步 0 |
| 6 | 在地图内容上生成三层高程，只用台阶零件，不破坏主路径与安全区平地 | 降档重铺；仍切断主路或出现非法落差则回退步 0 |
| 7 | 按可走格标记起点安全区 A / B，锁定禁刷 | — |
| 8 | 在允许的可走格放置奖励（含终点保底 1 个），校验互斥距离 | 奖励点不足则回退步 0 |
| 9 | 在允许的可走格生成怪物簇，守密度与精英后半段规则 | 密度或点位冲突则回退步 0 |
| 10 | 初始化迷雾：仅揭开起点安全区的内容与可走格；其余内容全黑；虚空不按范围整块揭开 | — |
| 11 | **最终全量校验**（§4 + §4.3 铁则 + 节奏 / 口袋 / 路标） | 任一失败，回退步 0 |

建议单张图最大重试 **24** 次；超限则换种子并打日志。禁止把失败图交给玩家。

未揭开的可走地不预加载怪物 / 奖励运行时节点；刷点数据可留在生成结果里，揭雾再实例化。已揭开区域永久保留。

---

## 10. 迷雾与运行时

| 项 | 规则 |
| --- | --- |
| 初始 | 除起点安全区外，地图内容全部黑色迷雾 |
| 视野 | 玩家周围圆形视野；平地默认半径 **8 格** |
| 高程修正 | 站在 +1 / +2 时视野半径 ×1.15 / ×1.25（可配） |
| 持久 | 已揭开的**地图内容**永久可见（墙和山也留在小地图上） |
| 加载 | 未揭开：不生成怪物 / 奖励实体，不跑其 AI |
| 战斗 | 揭雾后才允许该簇进入激活距离判定 |
| 剪影 | 小地图画已揭开的地图内容（地板 + 墙 + 山），必须是不规则块，禁止先显示一块大方场，也禁止只描一条细路 |

Tab 战术地图显示已揭开的地图内容；未揭开保持黑。安全屋仍可「看本层地图」——只揭示已生成的内容轮廓，不画出可生成范围，不提前加载实体。

---

## 11. 校验清单（生成器自测）

下列全部为硬失败。程序在最后一步逐条打勾，缺一条即整图作废。

1. §4.3.1 铁则 Q1–Q24 全部成立。  
2. 拓扑含必经节点 + 结构锚点；去掉可选边后主边连通，主路长度下降 ≤ 15%，轮廓不翻面。  
3. 所有几何在可生成范围内；正式场景 / 小地图不画范围边。  
4. 内容填充率 ∈ [0.62, 0.80]；可走 / 内容 ∈ [0.68, 0.85]。  
5. 内容贴边率 < 8%；矩形度 > 1.35；至少 3 个角落内容稀薄。  
6. 起点终点距离、象限、不相邻、不能一眼看见终点。  
7. 主通路连续、不自交；宏观结构只有一种；宽度连续；未打标处净宽 ≥ 3。  
8. 可走单连通，无可走孤岛；虚空只在内容外侧。  
9. 路线选择只含绕障 / 短回路 / 侧室；死胡同不像主路；入口有路标。  
10. 零件键全合法；无硬塞；高程只经台阶；出界不可走。  
11. 侧室各 1 通道回主路；数量 ∈ [0, 3]；与高档高程不同时堆满。  
12. 安全区、奖励间距、终点保底、怪物不压奖励、密度、精英后半段。  
13. 主路每 10–16 格有兴趣拍；空走 ≤ 18 格；每簇有 ≥ 6×6 战斗口袋。  
14. 迷雾仅安全区揭开；小地图是不规则内容块，不是细线也不是方块。  
15. 灰盒不套皮也能走完主线。

---

## 12. 建议配置（进 `data/`，不写死代码）

| 键 | 默认 | 含义 |
| --- | --- | --- |
| `gen_range_tiles` | 48 | 可生成范围边长（格）；不是玩家场地边长 |
| `content_fill_min` | 0.62 | 地图内容格 / 范围格下限 |
| `content_fill_max` | 0.80 | 地图内容格 / 范围格上限 |
| `content_fill_target` | 0.70 | 生成时优先靠近的内容填充率 |
| `walkable_of_content_min` | 0.68 | 可走格 / 内容格下限 |
| `walkable_of_content_max` | 0.85 | 可走格 / 内容格上限 |
| `walkable_of_content_target` | 0.78 | 生成时优先靠近的可走占内容比 |
| `content_edge_max` | 0.08 | 内容格贴范围最外圈的比例上限 |
| `content_rect_ratio_min` | 1.35 | 内容包围盒面积 / 内容格数，越大越不规则 |
| `corner_void_max_fill` | 0.20 | 角落 8×8 内内容占比上限（至少 3 角满足） |
| `path_width` | 4 | 主通道宽度 |
| `path_min_clear` | 3 | 最小可通行宽度 |
| `start_safe_a` | 6 | 禁怪半径 |
| `start_safe_b` | 12 | 禁奖励 / 精英 / 事件半径 |
| `start_end_min_factor` | 0.70 | 起点终点距离 / 可生成范围边长 |
| `branch_interval` | 9 | 主路径接口间距 |
| `loop_max` | 2 | 短回路上限；必须回到主路 |
| `loop_len_max_factor` | 0.25 | 回路长度 / 已走主路上限 |
| `detour_interval` | 12 | 主路绕障最小间距（格） |
| `optional_drop_path_max` | 0.15 | 丢掉可选边后主路长度最多下降 |
| `struct_anchor_min` | 2 | 结构锚点下限（防翻面） |
| `struct_anchor_max` | 4 | 结构锚点上限 |
| `beat_min_gap` | 10 | 主路兴趣拍最小间隔 |
| `beat_max_gap` | 16 | 主路兴趣拍最大间隔 |
| `empty_stretch_max` | 18 | 连续空走上限（格） |
| `combat_pocket` | 6 | 簇旁可走口袋边长下限 |
| `width_jump_max` | 2 | 相邻截面宽度差上限（未打标卡口） |
| `deadend_max` | 6 | 不像主路的死胡同最大深度 |
| `gen_success_floor` | 0.60 | 生成成功率警戒线 |
| `side_room_max` | 3 | 侧室上限 |
| `side_room_min_side` | 4 | 侧室最短边 |
| `side_room_max_side` | 10 | 侧室最长边 |
| `reward_min_dist` | 8 | 奖励互斥距离 |
| `mob_reward_min_dist` | 4 | 簇中心与奖励最小距离 |
| `mob_density_cap` | 0.12 | 怪物格 / **可走格**上限 |
| `fog_radius` | 8 | 平地揭雾半径 |
| `elev_fog_mul_1` | 1.15 | 低台揭雾系数 |
| `elev_fog_mul_2` | 1.25 | 高台揭雾系数 |
| `gen_max_retry` | 24 | 整图重试上限 |

数字进表，生成器只读规则。改手感先改表，不为单遗迹开一套生成类。

---

## 13. 与主文档的边界

- **52 场景名额不变**：本生成器产出的是一张关卡的逻辑布局实例；视觉仍套该遗迹零件库。不是 52 张整图，也不是无限大世界。  
- **日冕 / 营地不走本生成器**。  
- **簇编制、余晖上限、词缀、光阶**仍读《逆光设计文档》§3 / §9 / `data/`。  
- **镜头、Y 排序、手绘大块地面**仍读 §3.0.1；高程是逻辑层 + 分层绘景，不是可旋转 3D 高度场。  
- 若本表与主文档空间通式冲突：主路径 + 侧室必须仍能映射到「簇 + 可选精英门 + 主将庭院」。  
- 可生成范围不是主文档里的「场景边界美术」。玩家感知到的边界是地图内容外侧的山体 / 废墟 / 雾。墙和山属于地图，不属于范围虚空。  
- **地形零件怎么画、怎么交、什么不能画**：见 §15。仍服从主文档 §3.0.1 / §11.3 的尺寸与镜头合同。

---

## 14. 实现备忘（给程序）

1. 生成结果应是一份只读布局数据：拓扑图、零件键网格、可生成范围尺寸、地图内容掩码、可走掩码、墙/山/障碍掩码、虚空、房间多边形、路径折线、高程、刷点、迷雾掩码。场景节点在进层时实例化。  
2. 寻路只烤在可走格上（AStarGrid2D 或 NavigationRegion2D）。墙、山、障碍、虚空一律不可走；不要给可生成范围四边单独加一圈「世界墙」，除非调试。  
3. 种子：同一 `run_seed + floor_index + layout_id` 必须可复现。重试消耗子种子，不污染本局主种子序列。  
4. 调试叠加：拓扑图、零件键、可生成范围虚线、内容掩码、可走掩码、障碍、起点 A/B 圈、主路径中线、回路、侧室、奖励、簇、高程色块。正式模式必须能关掉范围虚线。  
5. 性能：范围默认 48×48 逻辑格，其中大部分应是地图内容，不是虚空；未揭雾不生成战斗实体。同屏上限仍受现有对象池约束。  
6. 验收先看：小地图是一块够大的不规则地；主线清楚且有局部选择；零件键无非法邻接；灰盒能走完；路上有节奏没有大段空走。一眼能读出「方块」「细路」「意大利面」或「哪都是兴趣点」，这张图作废。  
7. 统计每次生成的成功率和失败条款。成功率掉到 `gen_success_floor` 以下，先修零件库或铁则落地，不准加长重试次数。

---

## 15. 地形零件美术资源（制作合同）

生成器拼的是**零件键**，玩家看见的是**手绘皮**。皮画错，质量铁则守不住：接缝会漏、复读会喊、能走的地和看起来能走的地会对不上。主文档 §3.0.1 / §11.3 管画幅和镜头；本节管「给生成器用的地形组件」要注意什么。

### 15.1 先认清：画的是零件库，不是一张图

| 要交的 | 不要交的 |
| --- | --- |
| 每处遗迹 **一套**可复用零件（目标 80–150 张切片，见主文档） | 50 张完整关卡插画 |
| 每个高频键 **至少 3 个**外观变体 | 每块路都画成独一无二的英雄图 |
| 地标件（起点、主将庭院）可以独特 | 把地标画法用到普通直道上 |
| 锁定俯视投影的 2D 大块 | 3D 网格、可旋转斜视、32×32 农田瓦 |

十处遗迹换皮不换键。白石边境先把键集画全并跑通生成；其余遗迹只换物料气质（麦田废壕、密林、矿道…），接口集合必须相同，否则生成器要为每遗迹分叉。

### 15.2 一套零件库必须覆盖的族

缺族 = 生成时无匹配键 = 整图失败。每遗迹按族交齐，不要先堆装饰。

| 族 | 逻辑键大致要什么 | 美术注意 |
| --- | --- | --- |
| 地面填充 | 平地 / 低台 / 高台 | 大块手绘（1K–2K）。色调、光向全库统一，否则拼缝发脏 |
| 车道 | 直、弯、T、尽端；默认宽 4 格 | 路沿要能和墙/山接口咬齐。弯道必须是预做块，禁止直道旋 45° 冒充 |
| 墙 / 隔离墙 | 直、转角、尽端、开门（墙+墙合并结果） | 厚度画清楚；开门处地面对齐车道 |
| 山体 / 悬崖收边 | 内容外侧，不可走 | 这是小地图剪影的外缘。禁止收成四条直线。顶面可略探出键外，**碰撞仍以键为准** |
| 虚空过渡 | 雾、跌落、远景条 | 只贴在内容外侧。不挡走、不进寻路 |
| 台阶 | 0↔1、1↔2，正反方向 | 踏步必须画在可走多边形里。看起来能上、逻辑上不能上 = 废图 |
| 障碍 | 石、拒马、残柱等 | 明确标「挡走」或「纯装饰」。装饰不许吃碰撞 |
| 地标 | 起点缓冲、主将庭院、侧室门微光 | 独特可以。禁止复用到普通路段 |
| 装饰 | 草、旗、碎片 | 独立精灵，Y 排序。默认不挡走 |
| 病金变体 | 高层「被照及」 | 同键再出一套色，或共用一张 + 色调叠层。不另开 3D 天 |

建议逻辑块尺寸：**4×4 或 6×6 格**画成一块地面/车道，不要一格一块小瓦。墙和收边可以按边条交。这才能既像哈迪斯大图块，又给生成器足够的接口。

### 15.3 每一块皮必须跟三份数据一起交

没有这三份，程序不许进库。

| 附件 | 作用 |
| --- | --- |
| **零件键** | 四边 + 四角 + 高程层。文件名/资源里写死，例如 `r01_path_straight_n_v02` |
| **碰撞多边形** | 可走 / 不可走，与键一致。禁止用「画深一点」代替墙 |
| **Y 排序原点** | 角色从这块前面走过时谁遮谁。地面大块通常不参与遮挡；墙、山、柱要标脚点 |

另外建议附一张 **接口剪影条**（该边 8–16px 的颜色/形状样本），方便和下一块对缝。流放之路少地形缝，靠的就是邻块剪影被美术标过、生成器只抽对得上的块。

### 15.4 接缝、复读、投影：最容易翻的三件事

**接缝**

- 同一遗迹光向、地平线高度、地面明度必须锁定一份参考条。  
- 可走边与墙边的交界要画「路沿」，不要两块各画一半草地硬接。  
- 允许山石、草从键外探出 0.5–1 格，制造不规则外缘；探出部分不得变成新的可走地。  
- 房间重叠 1 格合并时，开门键的地面必须和两边车道是同一套路沿。

**复读**

- 直道、平地、直墙这些高频键：至少 3 变体，生成时按种子抽，相邻同键尽量不连用同一变体。  
- 变体只能改破损、苔藓、落叶、裂缝，**不能改接口形状**。改了接口就不是同一键。  
- 一块「特别好看的塌墙」如果当唯一墙用，整条廊会喊复读。塌、整、半塌要成套。  
- 地标件（带文字残片、独特雕塑）每张图最多出现 1 次，或只钉在节点上。

**投影**

- 按主文档锁死的俯视方向画，镜头旋转 1 度都不许。  
- 禁止把精灵旋转 90° 去补另一个朝向；该朝向要单独画，或只用左右镜像（镜像后光向仍要成立）。  
- 高台不是把平地缩小平贴。+1 / +2 要有厚度和挡墙，碰撞分层，角色走在上一层时 Y 排序不能穿帮。

### 15.5 能走和好看必须是同一件事

| 禁止 | 原因 |
| --- | --- |
| 地上画一条很像路的纹，逻辑却是墙 | 玩家会撞空气 |
| 逻辑可走，画面是悬崖或深坑 | 看起来像地形 bug |
| 装饰草堆实际挡住冲刺 | 装饰默认不挡；要挡就改成障碍键 |
| 台阶画了三级，键只有 0↔1 | 高程合同破裂 |
| 在皮上挖洞当侧室门，键上没开口 | 路标和接口双重失败 |

验收口诀：**灰盒能走的地方，套皮后看起来也该能走；灰盒不能走的，套皮后不该像路。**

### 15.6 目录、命名、性能

路径仍走内容域：`content/ruins/<遗迹>/`。源文件进 `source_assets/`，不进 Godot 导入。

```
content/ruins/r01_white_border/
  parts.tres            # 键 → 变体列表、碰撞、Y 原点
  ground/flat_v01.png
  path/straight_n_v01.png
  wall/open_ew_v01.png
  cliff/rim_s_v01.png
  stair/01_n_v01.png
  prop/stake_block_v01.png
  landmark/boss_court_v01.png
  overlay/sick_gold.tres
```

命名：`{族}_{键}_{朝向}_{变体}`。朝向写 `n/e/s/w` 或 `ew/ns`，不要叫 `final2`。  
同遗迹零件尽量打进同一图集、同一材质，少独立 `CanvasItem` 材质（主文档 §11.4）。地面 1K–2K，道具 256–1K，不上 4K 整图。

### 15.7 制作顺序（先能拼，再好看）

1. 只做白石边境：灰盒碰撞块 + 键表，生成器必须在无皮或纯色块下跑出合法图。  
2. 给高频键各画 1 个「素皮」，验收接缝和能走/好看一致。  
3. 高频键补到 3 变体；补齐山体收边、台阶、开门。  
4. 地标和装饰最后加。  
5. 病金变体随高层需求后置，但键不能变。  
6. 其余九处遗迹复制键表换皮，禁止私自加减接口。

一套键没齐就开画十处遗迹，生成成功率会掉到警戒线以下，那时不是加画能救的。

### 15.8 美术验收清单

1. 本遗迹 §15.2 各族都有至少 1 个合法键，高频键 ≥ 3 变体。  
2. 每块都有键、碰撞、Y 原点；装饰与障碍分得清。  
3. 同库光向、明度、路沿一致；邻块剪影能对上。  
4. 无 90° 旋 decal 冒充朝向；无 3D；无 32×32 铺满。  
5. 套皮后走灰盒同一条主线，不撞空气、不踩「假悬崖」。  
6. 山体收边是折线，不是方框；地标不在直道上复读。  
7. 切片数仍落在 80–150；图集合批。  
8. 换遗迹只换皮，键表与白石一致。

