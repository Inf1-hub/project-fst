# 项目协作规则

## 每次迭代必须更新日志

本规则适用于本仓库全部目录及所有参与开发的代理。迭代日志的唯一入口为 `docs/iteration_log.md`。

1. 开始修改前，阅读日志的最新条目，确认当前状态、未完成事项与已接受的用户要求。
2. 凡修改代码、地图、数值、资源、美术、开发环境、测试、设计文档或协作规则，都必须在同一次迭代中更新日志。仅讨论或只读调查、且没有项目改动时，可以不新增条目。
3. 同一个目标的一批连续修改合并为一次迭代；目标或交付范围发生明显变化时新增条目。未结束的工作更新原条目，不按工具调用次数拆分日志。
4. 使用递增编号 `ITER-001`、`ITER-002`……，最新条目放在最上方。记录实际日期；历史补录须标明补录日期，不猜测无法确认的实施时间。
5. 每条必须包含：目标/原因、实际改动、关键文件、验证结果、遗留问题/下一步、状态。采用日志底部的模板。
6. 验证必须如实记录执行命令、结果和适用范围；失败时记录关键错误及复现信息（如种子）。未执行写“未执行”并说明原因。仅文档修改可记录链接、内容检查，无须为此运行游戏测试。
7. 区分“已实现”和“已验证”。不得将计划写成完成项，不得用旧版本的通过结果覆盖当前失败，不得将局部测试通过描述为全部验收完成。
8. 若工作中断或被阻塞，离开前尽可能记录已落地改动、未执行步骤、阻塞原因及恢复入口。不要为了补日志绕过工具或权限限制。
9. 保留历史条目及当时的失败/限制。后续修复应在新条目中关联原编号；纠正历史事实时注明更正原因。
10. 提交、交付或结束本次修改前，检查日志与实际文件一致，并在最终回复中提供本次日志入口。**未同步迭代日志的修改，不视为本次迭代交付完成。**

日志是改动与验证记录，不替代设计文档。设计要求的存在不代表相应功能已经实现。
